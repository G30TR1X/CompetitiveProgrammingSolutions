#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define ull unsigned long long

void solve()
{
    ll n,m;
    cin >> n >> m;
    ll l[n+1], r[n+1], p[n+1], q[n+1];
    for (ll i = 1; i <= n; i++)
        cin >> l[i] >> r[i] >> p[i] >> q[i];
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    solve();

    return 0;
}
